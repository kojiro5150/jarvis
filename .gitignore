# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env*.local

# vercel
.vercel

# local project memory — generated at runtime from lib/memory/seed.ts,
# don't commit your local working state
/data/*.json

# local append-only execution audit
/.data/

# typescript
*.tsbuildinfo
next-env.d.ts

# Local secrets and runtime state
.env
.env.local
.env.*.local
data/google-tokens.json
data/memory.json

# Deployment-owned content retrieval policy (the tracked example is safe)
/config/content-retrieval-policy.local.json
/config/content-retrieval-policy.*.local.json

# Deployment-owned operational validation evidence. These reports may contain
# calendar content and engineering observations; only anonymised summaries are tracked.
/data/validation-reports/

# Build and dependency artefacts
node_modules/
.next/
/tmp/

# macOS
.DS_Store
