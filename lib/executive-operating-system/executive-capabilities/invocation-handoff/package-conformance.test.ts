import {describe,expect,it} from "vitest";import {readFileSync,readdirSync} from "node:fs";import {join} from "node:path";
describe("invocation handoff package conformance",()=>{it("imports only the immediately preceding routing publication and local contracts",()=>{const root=join(process.cwd(),"lib/executive-operating-system/executive-capabilities/invocation-handoff"),source=readdirSync(root).filter(x=>x.endsWith(".ts")&&!x.endsWith(".test.ts")).map(x=>readFileSync(join(root,x),"utf8")).join("\n");expect(source).toContain('from ".."');for(const prohibited of ["situational-awareness","/assessment","/deliberation","/intent","/planning","/reasoning","/proposal","connectors","browser","app/api","/execution","./invocation"])expect(source).not.toContain(prohibited)})});
