import {describe,expect,it} from "vitest";import {readFileSync} from "node:fs";import {join} from "node:path";
describe("invocation envelope publisher conformance",()=>{it("consumes only the canonical handoff and local envelope contracts",()=>{const source=readFileSync(join(process.cwd(),"lib/executive-operating-system/executive-capabilities/invocation/envelope.ts"),"utf8");expect(source).toContain('from "../invocation-handoff"');for(const prohibited of ["situational-awareness","../../../executive-context","/assessment","/deliberation","/intent","/planning","/reasoning","/proposal","../types","connectors","browser","app/api","invoker","registry"])expect(source).not.toContain(prohibited)})});
