import type { CandidatePlanComparisonPolicy,CandidatePlanComparisonRegistry } from "./types";
import { clone,compareText,deepFreeze,validatePolicy } from "./validation";
export class DeterministicCandidatePlanComparisonRegistry implements CandidatePlanComparisonRegistry{readonly #items=new Map<string,CandidatePlanComparisonPolicy>();constructor(items:readonly CandidatePlanComparisonPolicy[]=[]){items.forEach(x=>this.register(x))}register(policy:CandidatePlanComparisonPolicy):void{validatePolicy(policy);if(this.#items.has(policy.identifier))throw new Error(`duplicate candidate plan comparison policy identifier: ${policy.identifier}`);this.#items.set(policy.identifier,Object.freeze({...policy,metadata:deepFreeze(clone(policy.metadata)),supportedDimensionTypes:deepFreeze([...policy.supportedDimensionTypes]),applies:policy.applies.bind(policy),observe:policy.observe.bind(policy),...(policy.compare?{compare:policy.compare.bind(policy)}:{})}))}policies():readonly CandidatePlanComparisonPolicy[]{return Object.freeze([...this.#items.values()].sort((a,b)=>compareText(a.identifier,b.identifier)))}}
