import { describe,expect,it } from "vitest";
import { readFileSync,readdirSync } from "node:fs";
import { join } from "node:path";
describe("evaluation package conformance",()=>{it("keeps production dependencies within public upstream boundaries",()=>{const root=__dirname;const files=["types.ts","registry.ts","engine.ts","policies.ts","validation.ts","index.ts"];const source=files.map(f=>readFileSync(join(root,f),"utf8")).join("\n");expect(source).not.toMatch(/connectors|agents|app\/|api\/|runtime|provider|specialist|comparison|reasoning|execution-api/);const planning=join(root,"..");for(const entry of readdirSync(planning,{withFileTypes:true}))if(entry.isDirectory()&&entry.name!=="evaluation"){const text=readdirSync(join(planning,entry.name)).filter(f=>f.endsWith(".ts")&&!f.endsWith(".test.ts")).map(f=>readFileSync(join(planning,entry.name,f),"utf8")).join("\n");expect(text).not.toContain('from "../evaluation"')}});});
