import { expect,it } from "vitest"; import { cassieFixture,gmailRef,SYNTHETIC_FIXTURE_MARKER } from "./fixtures"; import { constructResponseEnvelope } from "./response-envelope"; import { validateResponseEnvelope } from "./validator";
it("produces an evidence-honest synthetic Cassie mixed-status envelope",()=>{const i=cassieFixture.input; const e=constructResponseEnvelope({envelopeId:"cassie:e",inputId:i.inputId,claims:i.claims.map(c=>({claimId:c.claimId,claimType:c.claimType,material:c.material,status:c.status,sourceReferences:c.sourceReferences,disclosure:c.status==="unsupported"?"Importance is not a governed claim.":undefined})),observedFacts:[{factId:"address",claimId:"contact",sourceReference:gmailRef,status:"available",ownership:"deterministic_observation",value:"cassie@example.invalid",contentKind:"metadata",boundedComplete:true}],interpretation:{ownership:"model_interpretation",text:"The operator may review the observed communication factors.",evidenceClaimIds:["contact"],uncertainty:"Significance is not governed."},uncertainties:[{uncertaintyId:"u",claimId:"importance",description:"Importance is unsupported."}],conflicts:[],refusal:{claimIds:["importance"],reason:"unsupported",explanation:"Heuristic importance is not evidence."}}); expect(cassieFixture.marker).toBe(SYNTHETIC_FIXTURE_MARKER); expect(e.overallStatus).toBe("unsupported"); expect(validateResponseEnvelope(i,e).valid).toBe(true); expect(i.compatibilityContext[0].excludedHeuristicFields).toContain("heuristicRanking"); expect(i.conversationHistory.every(t=>!t.canonicalEvidence)).toBe(true);});
