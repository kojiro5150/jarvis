import { constructGovernedConversationalInput } from "./input";
import type { GovernedClaimInput, GovernedConversationalInput, GovernedSourceReference } from "./types";
export const SYNTHETIC_FIXTURE_MARKER = "SYNTHETIC_GOVERNED_CONVERSATIONAL_FIXTURE_NOT_OPERATIONAL_EVIDENCE" as const;
export const REFERENCE_TIME = "2026-07-15T12:00:00.000Z";
export const gmailRef: GovernedSourceReference = { sourceId: "synthetic:gmail", resourceId: "synthetic:message:1", field: "from.address", observedAt: REFERENCE_TIME };
const base = (overrides: Partial<GovernedClaimInput>): GovernedClaimInput => ({ claimId: "contact", claimType: "contact_address_lookup", material: true, status: "available", ownership: "deterministic_observation", sourceReferences: [gmailRef], factualValues: ["cassie@example.invalid"], sourceAvailable: true, provenance: SYNTHETIC_FIXTURE_MARKER, observedAt: REFERENCE_TIME, contentKind: "metadata", boundedComplete: true, conflicts: [], ...overrides });
const make = (id: string, claims: readonly GovernedClaimInput[], compatibilityContext: GovernedConversationalInput["compatibilityContext"] = []): GovernedConversationalInput => constructGovernedConversationalInput({ inputId: id, threadId: `thread:${id}`, requestId: `request:${id}`, exchangeId: `exchange:${id}`, projectionId: "projection:synthetic", projectionLineage: { threadId: `thread:${id}`, requestId: `request:${id}`, exchangeId: `exchange:${id}`, projectionId: "projection:synthetic" }, referenceTime: REFERENCE_TIME, question: { text: "what's Cassie's email? anything important?", timezone: "Etc/UTC", locale: "en-US" }, claims, sources: [{ sourceId: "synthetic:gmail", available: claims.every(c => c.sourceAvailable), status: claims.some(c => c.status === "unavailable") ? "unavailable" : "available", observedAt: REFERENCE_TIME, provenance: SYNTHETIC_FIXTURE_MARKER }], compatibilityContext, conversationHistory: [{ turnId: "turn:operator", classification: "operator_provided", content: "What is Cassie's address?" }, { turnId: "turn:assistant", classification: "assistant_prior_output", content: "I previously guessed old@example.invalid" }] });
export const availableGovernedCommunicationFixture = { marker: SYNTHETIC_FIXTURE_MARKER, input: make("available", [base({})]) };
const conflict = { conflictId: "conflict:address", claimId: "contact", governedReference: gmailRef, compatibilityContextId: "legacy:1", description: "Legacy display address disagrees." };
export const governedLegacyConflictFixture = { marker: SYNTHETIC_FIXTURE_MARKER, input: make("conflict", [base({ conflicts: [conflict] })], [{ contextId: "legacy:1", legacySource: "OperationalState.gmailThreads", governedResourceId: gmailRef.resourceId, classification: "compatibility", authority: "none", descriptiveFields: { from: "Cassie <old@example.invalid>" }, excludedHeuristicFields: ["important", "needsReply"] }]) };
export const unavailableFallbackFixture = { marker: SYNTHETIC_FIXTURE_MARKER, input: make("unavailable", [base({ status: "unavailable", sourceAvailable: false, factualValues: [] })], [{ contextId: "legacy:fallback", legacySource: "local fallback", classification: "fallback", authority: "none", descriptiveFields: { snippet: "cached excerpt" }, excludedHeuristicFields: ["important"] }]) };
export const unsupportedImportanceFixture = { marker: SYNTHETIC_FIXTURE_MARKER, input: make("importance", [base({ claimId: "importance", claimType: "message_importance", status: "unsupported", ownership: "unsupported", factualValues: [], sourceReferences: [] })]) };
export const insufficientContactIdentityFixture = { marker: SYNTHETIC_FIXTURE_MARKER, input: make("ambiguous", [base({ status: "insufficient_coverage", factualValues: [] })]) };
export const snippetOnlyFixture = { marker: SYNTHETIC_FIXTURE_MARKER, input: make("snippet", [base({ claimType: "message_excerpt", contentKind: "partial_excerpt", factualValues: ["partial text"] })]) };
export const priorAssistantEvidenceAttemptFixture = { marker: SYNTHETIC_FIXTURE_MARKER, input: make("assistant", [base({ sourceReferences: [], status: "insufficient_coverage", factualValues: [] })]) };
export const cassieFixture = { marker: SYNTHETIC_FIXTURE_MARKER, input: make("cassie", [base({}), base({ claimId: "importance", claimType: "message_importance", status: "unsupported", ownership: "unsupported", sourceReferences: [], factualValues: [], contentKind: "metadata" })], [{ contextId: "legacy:cassie", legacySource: "OperationalState.gmailThreads", governedResourceId: gmailRef.resourceId, classification: "compatibility", authority: "none", descriptiveFields: { subject: "Synthetic update", from: "Cassie", snippet: "Partial synthetic excerpt", unread: true, sourceLabel: "inbox" }, excludedHeuristicFields: ["important", "needsReply", "heuristicRanking"] }]) };
