import { aggregateEvidenceStatus } from "./evidence-status";
import type { CompatibilityContext, GovernedClaimInput, GovernedConversationalInput, GovernedConversationTurn, GovernedQuestion, GovernedSourceInput } from "./types";
export interface GovernedInputConstruction { inputId: string; runId: string; sessionId: string; interfaceContractId: string; projectionId: string; referenceTime: string; question: GovernedQuestion; claims: readonly GovernedClaimInput[]; sources: readonly GovernedSourceInput[]; compatibilityContext?: readonly CompatibilityContext[]; conversationHistory?: readonly Omit<GovernedConversationTurn, "canonicalEvidence">[]; }
export function constructGovernedConversationalInput(value: GovernedInputConstruction): GovernedConversationalInput { if (!value.referenceTime || Number.isNaN(Date.parse(value.referenceTime))) throw new Error("referenceTime must be an explicit ISO instant"); const compatibilityContext = (value.compatibilityContext ?? []).map(c => ({ ...c, authority: "none" as const })); const conversationHistory = (value.conversationHistory ?? []).map(t => ({ ...t, canonicalEvidence: false as const })); const claims = value.claims.map(claim => { const conflicts = claim.conflicts; return conflicts.length && claim.status === "available" ? { ...claim, status: "insufficient_coverage" as const } : claim; }); return { ...value, claims, compatibilityContext, conversationHistory, overallStatus: aggregateEvidenceStatus(claims) }; }
