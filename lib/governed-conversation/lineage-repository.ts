import type { ConversationalLifecycleEvent } from "./exchange-lifecycle";
import type { ConversationalExecutionRecord, ConversationalExchange, ConversationalRequest, ConversationalRequestRejectionRecord, ConversationalThread, ModelInvocationAttempt, ValidatedConversationalResponseEnvelopeReference } from "./lineage-types";
import type { GovernedConversationalProjection, GovernedInputReference } from "./projection-composer";
export type CommitFailure = "conflict" | "duplicate" | "invalid_transition" | "lineage_mismatch" | "terminal_record_exists" | "repository_unavailable";
export type CommitResult = { readonly committed: true; readonly commitId: string; readonly committedAt: string } | { readonly committed: false; readonly failure: CommitFailure; readonly detail: string };
export interface ConversationalExchangeAggregate { readonly exchange: ConversationalExchange; readonly projection?: GovernedConversationalProjection; readonly governedInputReference?: GovernedInputReference; readonly attempts: readonly ModelInvocationAttempt[]; readonly envelope?: ValidatedConversationalResponseEnvelopeReference; readonly lifecycleEvents: readonly ConversationalLifecycleEvent[]; readonly terminalRecord?: ConversationalExecutionRecord }
export interface ConversationalLineageRepository { createThread(thread: ConversationalThread): Promise<CommitResult>; closeThread(threadId: string, closedAt: string): Promise<CommitResult>; commitAcceptedRequest(request: ConversationalRequest): Promise<CommitResult>; commitRequestRejection(rejection: ConversationalRequestRejectionRecord): Promise<CommitResult>; createExchange(exchange: ConversationalExchange): Promise<CommitResult>; commitProjection(exchangeId: string, projection: GovernedConversationalProjection, governedInputReference: GovernedInputReference): Promise<CommitResult>; startAttempt(attempt: ModelInvocationAttempt): Promise<CommitResult>; completeAttempt(attempt: ModelInvocationAttempt): Promise<CommitResult>; commitValidatedEnvelope(envelope: ValidatedConversationalResponseEnvelopeReference): Promise<CommitResult>; transitionExchange(event: ConversationalLifecycleEvent): Promise<CommitResult>; commitTerminalRecord(record: ConversationalExecutionRecord): Promise<CommitResult>; getThread(threadId: string): Promise<ConversationalThread | null>; getRequest(requestId: string): Promise<ConversationalRequest | null>; getExchange(exchangeId: string): Promise<ConversationalExchangeAggregate | null>; getByIdempotencyKey(idempotencyKey: string): Promise<ConversationalRequest | null> }
