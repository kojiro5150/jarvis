import { describe, expect, it } from "vitest"; import { determineRetryDisposition, type ConversationalAttemptContext } from "./retry-policy";
const context: ConversationalAttemptContext={requestId:"r",operatorQuestionDigest:"q",projectionId:"p",governedInputId:"i",evidenceDigest:"e",sourceAvailabilityDigest:"a",claimSetDigest:"c",claimMaterialityDigest:"m",referenceTime:"t",compatibilityContextDigest:"x",evidenceRulesetId:"er",claimClassificationRulesetId:"cr",validationPolicyId:"v",providerConfigurationReference:"provider:a",failoverPolicyId:"failover:1",preauthorisedEquivalentProviderConfigurations:["provider:b"]};
describe("Retry Model A",()=>{ it("keeps unchanged governed meaning in one exchange with a new attempt",()=>expect(determineRetryDisposition(context,{...context})).toEqual({kind:"same_exchange_new_attempt"})); it("returns every changed semantic boundary",()=>expect(determineRetryDisposition(context,{...context,operatorQuestionDigest:"q2",evidenceDigest:"e2",claimSetDigest:"c2",referenceTime:"t2",validationPolicyId:"v2"})).toEqual({kind:"new_exchange_required",reasons:["operatorQuestionDigest","evidenceDigest","claimSetDigest","referenceTime","validationPolicyId"]})); it("allows only explicitly preauthorised equivalent provider failover",()=>{expect(determineRetryDisposition(context,{...context,providerConfigurationReference:"provider:b"}).kind).toBe("same_exchange_new_attempt"); expect(determineRetryDisposition(context,{...context,providerConfigurationReference:"provider:c"})).toEqual({kind:"new_exchange_required",reasons:["providerConfigurationReference"]});}); });
