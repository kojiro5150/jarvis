export interface ConversationalAttemptContext { readonly requestId: string; readonly operatorQuestionDigest: string; readonly projectionId: string; readonly governedInputId: string; readonly evidenceDigest: string; readonly sourceAvailabilityDigest: string; readonly claimSetDigest: string; readonly claimMaterialityDigest: string; readonly referenceTime: string; readonly compatibilityContextDigest: string; readonly evidenceRulesetId: string; readonly claimClassificationRulesetId: string; readonly validationPolicyId: string; readonly providerConfigurationReference: string; readonly failoverPolicyId?: string; readonly preauthorisedEquivalentProviderConfigurations?: readonly string[] }
export type RetryBoundaryReason = keyof Omit<ConversationalAttemptContext, "preauthorisedEquivalentProviderConfigurations" | "failoverPolicyId">;
export type RetryDisposition = { readonly kind: "same_exchange_new_attempt" } | { readonly kind: "new_exchange_required"; readonly reasons: readonly RetryBoundaryReason[] };
const fields: readonly RetryBoundaryReason[] = ["requestId", "operatorQuestionDigest", "projectionId", "governedInputId", "evidenceDigest", "sourceAvailabilityDigest", "claimSetDigest", "claimMaterialityDigest", "referenceTime", "compatibilityContextDigest", "evidenceRulesetId", "claimClassificationRulesetId", "validationPolicyId", "providerConfigurationReference"];
export function determineRetryDisposition(previous: ConversationalAttemptContext, next: ConversationalAttemptContext): RetryDisposition { const reasons = fields.filter((field) => previous[field] !== next[field]); const provider = reasons.indexOf("providerConfigurationReference"); if (provider >= 0 && previous.failoverPolicyId && previous.failoverPolicyId === next.failoverPolicyId && previous.preauthorisedEquivalentProviderConfigurations?.includes(next.providerConfigurationReference)) reasons.splice(provider, 1); return reasons.length ? { kind: "new_exchange_required", reasons } : { kind: "same_exchange_new_attempt" }; }
