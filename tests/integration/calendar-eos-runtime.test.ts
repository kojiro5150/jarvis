import { describe,expect,it } from "vitest";
import { normalizeGoogleEvent } from "../../lib/connectors/calendar-event";
import { CalendarProjectionAdapter } from "../../lib/executive-operating-system/situational-awareness/projection";
import { createSituationalAwareness } from "../../lib/executive-operating-system/situational-awareness/model";
import { createSituationalAwarenessSnapshot } from "../../lib/executive-operating-system/situational-awareness/lifecycle";
import { DeterministicExecutiveOperatingSystemRuntime } from "../../lib/executive-operating-system/runtime";
describe("Calendar adapter to EOS compatibility",()=>{it("accepts a frozen adapter artifact without a live connector",async()=>{const events=Object.freeze([{...normalizeGoogleEvent({id:"fictional-board",summary:"Fictional board review",start:{dateTime:"2031-02-03T10:00:00Z"},end:{dateTime:"2031-02-03T11:00:00Z"}},0,{calendarId:"fictional-calendar",calendarName:"Fictional Calendar"}),status:"confirmed" as const}]);const adapter=new CalendarProjectionAdapter({identity:{userId:"fictional",displayName:"Fictional Executive"},observedAt:"2031-02-01T09:00:00Z",connector:{source:"google",listUpcoming:async()=>events}});const artifact=await adapter.project();const previous=createSituationalAwarenessSnapshot({snapshotId:"calendar-before",observedAt:"2031-02-01T08:00:00Z",state:createSituationalAwareness({identity:{userId:"fictional",displayName:"Fictional Executive"},roles:[],projects:[],commitments:[],waitingItems:[],priorities:[],activeWork:[],sources:[{id:artifact.provenance.sourceId,kind:"calendar",status:"available",observedAt:"2031-02-01T08:00:00Z"}]})});const result=new DeterministicExecutiveOperatingSystemRuntime().run({projectionArtifacts:{artifacts:[artifact],previousSnapshot:previous,snapshotId:"calendar-current",observedAt:"2031-02-01T09:00:00Z"},configuration:{intent:{objectives:[]},constraints:{constraints:[]},candidatePlanDefinitions:[],evaluationDefinitions:[],comparisonDefinitions:[],reasoningDefinitions:[],proposalDefinitions:[]}});expect(result.proposals.proposalSetId).toMatch(/^governed-action-proposal-set:/);expect(Object.isFrozen(events)).toBe(true)})});
